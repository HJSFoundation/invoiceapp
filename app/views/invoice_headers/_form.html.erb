<%= form_for(@invoice_header) do |f| %>
  <% if @invoice_header.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invoice_header.errors.count, "error") %> prohibited this invoice_header from being saved:</h2>

      <ul>
      <% @invoice_header.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Invoice Number: <%= @invoice_header.id.to_s.rjust(6, '0') %>
  </div>
  <div class="field">
    <%= f.label :customer_id %>
    <%= collection_select :invoice_header, :customer_id, Customer.all, :id, :name %>
  </div>
<!--
  <div class="field">
    <%= f.label :invoice_total %><br />
    <%= f.text_field :invoice_total %>
  </div>
-->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
